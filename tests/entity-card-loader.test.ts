// Removed: entity-card-loader.test.ts. Card construction moved to
// src/a2a/card.ts and src/entity-card-loader.ts now only handles keypair
// resolution. New tests will live under tests/a2a/* once we add them.
import { test } from "vitest";
test.skip("legacy entity-card-loader tests removed", () => {});
