// Removed: webhook.test.ts. The WebhookCommunicationManager has been
// replaced by the A2A server. New tests will live under tests/a2a/*.
import { test } from "vitest";
test.skip("legacy webhook tests removed", () => {});
